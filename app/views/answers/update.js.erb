var id = '<%= @answer.id %>'
var errors = document.querySelector(`#answer-${id}-errors`)
errors.innerHTML = ("<%= render 'shared/errors', resource: @answer %>")

<% if @answer.valid? %>
	document.querySelector(`#answer-${id}`).innerHTML = "<%= j render @answer %>"	
<% end %>
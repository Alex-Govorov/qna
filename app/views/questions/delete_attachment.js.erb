var attachments = document.querySelector('.question-attachments')
attachments.addEventListener("ajax:success", (event) => {
	attachments.innerHTML = "<%= j render 'shared/files', resource: @question %>"	
 })

h1= @question.title
p= @question.body
table
  thead
    tr
      th= t('.answers')
      th= t('.author')
  tbody class="answers" = render @answers
div class="answer-errors"
= render partial: 'answers/form', locals: { question: @question, answer: Answer.new }
